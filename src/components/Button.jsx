import * as React from 'react';
import Button from '@mui/material/Button';

export default function Buttons({ printValue }) {
    return (

        <Button variant="contained">Generar</Button>


    );
}